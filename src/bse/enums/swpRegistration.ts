// Transaction Mode
export enum SWPTransMode {
  DEMAT = 'D',
  PHYSICAL = 'P',
}

// Frequency Type
export enum SWPFrequencyType {
  WEEKLY = 'Weekly',
  MONTHLY = 'Monthly',
  QUARTERLY = 'Quarterly',
}
